# Spikes A + B — x402 (pay-to-share / pay-to-query) + ap2 (invoice envelope)
# Verified 2026-05-31. requires Python >=3.10.
# Create venv with: uv venv --python 3.11 .venv
x402[httpx,evm]==2.12.0
ap2==0.1.1
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
eth-account>=0.13
python-dotenv>=1.0
