# Generate with:
# python3 -c "from eth_account import Account; a=Account.create(); print('KEY=', a.key.hex()); print('ADDR=', a.address)"
EVM_PRIVATE_KEY=0x_replace_me
EVM_RECEIVE_ADDRESS=0x_replace_me
RESOURCE_URL=http://localhost:4021/publish-info
# Faucets (Base Sepolia testnet):
# - ETH (gas): https://www.alchemy.com/faucets/base-sepolia
# - USDC:       https://faucet.circle.com (no signup)
